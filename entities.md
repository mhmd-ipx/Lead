# 📋 موجودیت‌های پروژه LEAD

این سند شامل تمام جداول و موجودیت‌های مورد نیاز برای پروژه است.

---

## 1️⃣ کاربران و احراز هویت

### User (کاربر)
- `id` - شناسه یکتا
- `phone` - شماره موبایل (برای لاگین)
- `name` - نام کاربر
- `email` - ایمیل (اختیاری)
- `avatar` - عکس پروفایل (اختیاری)
- `role` - نقش کاربر (admin, owner, manager)
- `status` - وضعیت (active, inactive)
- `created_at` - تاریخ ثبت‌نام
- `last_login` - آخرین ورود

**توضیحات:**
- لاگین با OTP (کد یکبار مصرف) انجام میشه
- هر کاربر یکی از سه نقش admin, owner یا manager رو داره

---

## 2️⃣ سازمان‌ها

### Company (سازمان/شرکت)
- `id` - شناسه یکتا
- `name` - نام تجاری
- `legal_name` - نام رسمی/ثبتی
- `phone` - تلفن
- `email` - ایمیل
- `address` - آدرس
- `national_id` - شناسه ملی/شناسه ثبت
- `economic_code` - کد اقتصادی
- `field_of_activity` - حوزه فعالیت
- `logo` - لوگو (اختیاری)
- `website` - وب‌سایت (اختیاری)
- `description` - توضیحات (اختیاری)
- `owner_id` - شناسه مالک (ارتباط با User)
- `status` - وضعیت (active, inactive)
- `created_at` - تاریخ ایجاد

**توضیحات:**
- هر سازمان یک مالک (owner) داره
- مالک میتونه متقاضیان (managers) رو مدیریت کنه

---

## 3️⃣ متقاضیان

### Manager (متقاضی/مدیر)
- `id` - شناسه یکتا
- `user_id` - شناسه کاربر (ارتباط با User)
- `company_id` - شناسه سازمان (ارتباط با Company)
- `name` - نام
- `email` - ایمیل
- `phone` - شماره موبایل
- `position` - سمت شغلی
- `department` - بخش/دپارتمان
- `status` - وضعیت (active, inactive)
- `assessment_status` - وضعیت نیازسنجی (not_started, incomplete, completed)
- `exam_status` - وضعیت آزمون (not_started, in_progress, completed)
- `can_view_results` - آیا میتونه نتایج رو ببینه؟ (true/false)
- `avatar` - عکس (اختیاری)
- `created_at` - تاریخ ثبت

**توضیحات:**
- متقاضیان توسط owner ثبت میشن
- هر متقاضی میتونه نیازسنجی و آزمون بده

---

## 4️⃣ نیازسنجی‌ها (Assessment)

### AssessmentTemplate (قالب نیازسنجی)
- `id` - شناسه یکتا
- `name` - نام قالب
- `description` - توضیحات
- `category` - دسته‌بندی
- `estimated_time` - زمان تقریبی (به دقیقه)
- `status` - وضعیت (active, draft, archived)
- `created_by` - ایجاد شده توسط (admin)
- `created_at` - تاریخ ایجاد
- `updated_at` - تاریخ بروزرسانی

**توضیحات:**
- ادمین قالب‌های نیازسنجی رو میسازه
- هر قالب شامل چندین مرحله (step) هست

### AssessmentStep (مرحله نیازسنجی)
- `id` - شناسه یکتا
- `template_id` - شناسه قالب (ارتباط با AssessmentTemplate)
- `title` - عنوان مرحله
- `description` - توضیحات (اختیاری)
- `order` - ترتیب نمایش
- `created_at` - تاریخ ایجاد

**توضیحات:**
- هر قالب میتونه چندین مرحله داشته باشه
- هر مرحله شامل چندین سوال هست

### AssessmentQuestion (سوال نیازسنجی)
- `id` - شناسه یکتا
- `step_id` - شناسه مرحله (ارتباط با AssessmentStep)
- `question` - متن سوال
- `type` - نوع سوال (text, select, radio, checkbox, rating)
- `options` - گزینه‌ها (برای select, radio, checkbox) - JSON
- `required` - اجباری؟ (true/false)
- `order` - ترتیب نمایش
- `created_at` - تاریخ ایجاد

**توضیحات:**
- انواع سوال: متنی، انتخابی، چندگزینه‌ای، امتیازدهی
- options فقط برای سوالات انتخابی پر میشه

### Assessment (نیازسنجی تکمیل شده)
- `id` - شناسه یکتا
- `manager_id` - شناسه متقاضی (ارتباط با Manager)
- `template_id` - شناسه قالب (ارتباط با AssessmentTemplate)
- `current_step` - مرحله فعلی
- `answers` - پاسخ‌ها - JSON (شامل تمام پاسخ‌ها)
- `status` - وضعیت (draft, submitted)
- `score` - امتیاز (اختیاری)
- `created_at` - تاریخ شروع
- `updated_at` - تاریخ آخرین ویرایش
- `submitted_at` - تاریخ ثبت نهایی (اختیاری)

**توضیحات:**
- وقتی owner نیازسنجی رو برای متقاضی ایجاد میکنه، این رکورد ساخته میشه
- پاسخ‌ها به صورت JSON ذخیره میشن

---

## 5️⃣ آزمون‌ها (Exams)

### Exam (آزمون)
- `id` - شناسه یکتا
- `title` - عنوان آزمون
- `description` - توضیحات
- `duration` - مدت زمان (به دقیقه)
- `total_questions` - تعداد کل سوالات
- `passing_score` - حداقل نمره قبولی
- `status` - وضعیت (active, draft, archived)
- `created_by` - ایجاد شده توسط (admin)
- `created_at` - تاریخ ایجاد
- `updated_at` - تاریخ بروزرسانی

**توضیحات:**
- ادمین آزمون‌ها رو میسازه
- هر آزمون شامل چندین سوال از بانک سوال هست

### Question (سوال آزمون)
- `id` - شناسه یکتا
- `question` - متن سوال
- `type` - نوع سوال (multiple_choice, true_false, descriptive)
- `options` - گزینه‌ها (برای چند گزینه‌ای) - JSON
- `correct_answer` - پاسخ صحیح
- `score` - امتیاز سوال
- `difficulty` - سختی (easy, medium, hard)
- `category` - دسته‌بندی
- `created_by` - ایجاد شده توسط (admin)
- `created_at` - تاریخ ایجاد
- `updated_at` - تاریخ بروزرسانی

**توضیحات:**
- بانک سوال مستقل از آزمون‌ها هست
- ادمین میتونه سوالات رو به آزمون‌ها اضافه کنه

### ExamQuestion (رابطه آزمون و سوالات)
- `id` - شناسه یکتا
- `exam_id` - شناسه آزمون (ارتباط با Exam)
- `question_id` - شناسه سوال (ارتباط با Question)
- `order` - ترتیب نمایش سوال در آزمون

**توضیحات:**
- این جدول رابطه many-to-many بین آزمون و سوالات رو مدیریت میکنه

### ExamSet (مجموعه آزمون)
- `id` - شناسه یکتا
- `manager_id` - شناسه متقاضی (ارتباط با Manager)
- `assessment_id` - شناسه نیازسنجی (ارتباط با Assessment) - اختیاری
- `title` - عنوان مجموعه
- `description` - توضیحات
- `assigned_date` - تاریخ تخصیص
- `exam_date` - تاریخ برگزاری (اختیاری)
- `due_date` - مهلت پایان
- `status` - وضعیت (pending, in_progress, completed)
- `username` - نام کاربری برای متقاضی
- `password` - رمز عبور موقت
- `created_at` - تاریخ ایجاد

**توضیحات:**
- ادمین بعد از مشاهده نیازسنجی، چند آزمون رو به متقاضی تخصیص میده
- متقاضی با username و password میتونه وارد بشه و آزمون بده

### ExamSetItem (آیتم‌های مجموعه آزمون)
- `id` - شناسه یکتا
- `exam_set_id` - شناسه مجموعه (ارتباط با ExamSet)
- `exam_id` - شناسه آزمون (ارتباط با Exam)
- `order` - ترتیب
- `status` - وضعیت (not_started, in_progress, completed)

**توضیحات:**
- این جدول رابطه many-to-many بین ExamSet و Exam هست

### ExamResult (نتیجه آزمون)
- `id` - شناسه یکتا
- `exam_set_id` - شناسه مجموعه آزمون (ارتباط با ExamSet)
- `exam_id` - شناسه آزمون (ارتباط با Exam)
- `manager_id` - شناسه متقاضی (ارتباط با Manager)
- `answers` - پاسخ‌های داده شده - JSON
- `score` - امتیاز کسب شده
- `total_score` - امتیاز کل
- `percentage` - درصد
- `status` - وضعیت (passed, failed, in_progress)
- `started_at` - زمان شروع
- `completed_at` - زمان اتمام (اختیاری)
- `time_spent` - زمان صرف شده (به ثانیه)

**توضیحات:**
- برای هر آزمون که متقاضی میده، یک نتیجه ثبت میشه
- پاسخ‌ها به صورت JSON نگهداری میشن

---

## 6️⃣ امور مالی

### Payment (پرداخت)
- `id` - شناسه یکتا
- `manager_id` - شناسه متقاضی (ارتباط با Manager)
- `company_id` - شناسه سازمان (ارتباط با Company)
- `amount` - مبلغ
- `currency` - واحد پول (IRR, USD, ...)
- `type` - نوع پرداخت (before_results, after_results)
- `status` - وضعیت (pending, paid, failed, refunded)
- `payment_method` - روش پرداخت (online, card, cash, ...)
- `transaction_id` - شناسه تراکنش (از درگاه پرداخت)
- `payment_date` - تاریخ پرداخت (اختیاری)
- `due_date` - مهلت پرداخت (اختیاری)
- `created_at` - تاریخ ایجاد

**توضیحات:**
- دو نوع پرداخت داریم: قبل از نتایج و بعد از نتایج
- owner میتونه برای هر متقاضی یا همه متقاضیان پرداخت کنه

### Invoice (فاکتور)
- `id` - شناسه یکتا
- `payment_id` - شناسه پرداخت (ارتباط با Payment)
- `invoice_number` - شماره فاکتور
- `amount` - مبلغ
- `currency` - واحد پول
- `status` - وضعیت (requested, generated, sent)
- `request_date` - تاریخ درخواست
- `generated_date` - تاریخ صدور (اختیاری)
- `pdf_url` - لینک فایل PDF (اختیاری)
- `created_at` - تاریخ ایجاد

**توضیحات:**
- owner میتونه برای پرداخت‌ها فاکتور درخواست کنه
- ادمین فاکتور رو تولید و ارسال میکنه

### FinancialDocument (سند مالی)
- `id` - شناسه یکتا
- `payment_id` - شناسه پرداخت (ارتباط با Payment) - اختیاری
- `title` - عنوان سند
- `amount` - مبلغ
- `currency` - واحد پول
- `type` - نوع سند (income, expense)
- `status` - وضعیت (pending, paid, cancelled)
- `description` - توضیحات (اختیاری)
- `created_date` - تاریخ ایجاد
- `paid_date` - تاریخ پرداخت (اختیاری)

**توضیحات:**
- اسناد مالی جزئیات مالی رو نگهداری میکنن

### Bill (صورتحساب)
- `id` - شناسه یکتا
- `company_id` - شناسه سازمان (ارتباط با Company)
- `bill_number` - شماره صورتحساب
- `total_amount` - مبلغ کل
- `currency` - واحد پول
- `status` - وضعیت (pending, paid, partially_paid, cancelled)
- `due_date` - مهلت پرداخت
- `paid_date` - تاریخ پرداخت (اختیاری)
- `official_invoice_requested` - درخواست فاکتور رسمی؟ (true/false)
- `official_invoice_pdf_url` - لینک فاکتور رسمی (اختیاری)
- `description` - توضیحات (اختیاری)
- `created_at` - تاریخ ایجاد

**توضیحات:**
- صورتحساب‌ها میتونن شامل چند سند مالی باشن

### BillItem (آیتم‌های صورتحساب)
- `id` - شناسه یکتا
- `bill_id` - شناسه صورتحساب (ارتباط با Bill)
- `financial_document_id` - شناسه سند مالی (ارتباط با FinancialDocument)

**توضیحات:**
- این جدول رابطه many-to-many بین Bill و FinancialDocument هست

---

## 7️⃣ ارتباطات

### Notification (اعلان)
- `id` - شناسه یکتا
- `user_id` - شناسه کاربر (ارتباط با User)
- `title` - عنوان
- `message` - متن پیام
- `type` - نوع (system, payment, exam, assessment, support)
- `is_read` - خوانده شده؟ (true/false)
- `action_url` - لینک عملیات (اختیاری)
- `created_at` - تاریخ ایجاد

**توضیحات:**
- اعلان‌ها برای کاربران ارسال میشن
- میتونن لینک به بخش خاصی از سیستم داشته باشن

### SupportTicket (تیکت پشتیبانی)
- `id` - شناسه یکتا
- `user_id` - شناسه کاربر (ارتباط با User)
- `ticket_number` - شماره تیکت
- `subject` - موضوع
- `category` - دسته‌بندی (technical, financial, general, ...)
- `priority` - اولویت (low, medium, high, urgent)
- `status` - وضعیت (open, in_progress, waiting_for_user, closed)
- `created_at` - تاریخ ایجاد
- `updated_at` - تاریخ آخرین بروزرسانی
- `closed_at` - تاریخ بسته شدن (اختیاری)

**توضیحات:**
- کاربران میتونن تیکت پشتیبانی بزنن
- ادمین به تیکت‌ها پاسخ میده

### SupportMessage (پیام پشتیبانی)
- `id` - شناسه یکتا
- `ticket_id` - شناسه تیکت (ارتباط با SupportTicket)
- `user_id` - شناسه ارسال کننده (ارتباط با User)
- `message` - متن پیام
- `type` - نوع (user, admin)
- `attachments` - فایل‌های پیوست (اختیاری) - JSON
- `is_read` - خوانده شده؟ (true/false)
- `created_at` - تاریخ ارسال

**توضیحات:**
- هر تیکت میتونه چندین پیام داشته باشه
- پیام‌ها میتونن فایل پیوست داشته باشن

### SMSLog (لاگ پیامک)
- `id` - شناسه یکتا
- `user_id` - شناسه کاربر (ارتباط با User)
- `phone` - شماره موبایل
- `message` - متن پیام
- `type` - نوع (otp, notification, reminder, ...)
- `status` - وضعیت (pending, sent, failed)
- `provider` - ارائه دهنده (kavenegar, ...)
- `provider_message_id` - شناسه پیام از سمت ارائه دهنده
- `sent_at` - تاریخ ارسال (اختیاری)
- `created_at` - تاریخ ایجاد

**توضیحات:**
- تمام پیامک‌های ارسالی لاگ میشن

### MessageTemplate (قالب پیام)
- `id` - شناسه یکتا
- `name` - نام قالب
- `type` - نوع (sms, email, notification)
- `subject` - موضوع (برای ایمیل)
- `content` - محتوای قالب (با متغیرها)
- `variables` - متغیرهای قابل استفاده - JSON
- `status` - وضعیت (active, inactive)
- `created_by` - ایجاد شده توسط (admin)
- `created_at` - تاریخ ایجاد
- `updated_at` - تاریخ بروزرسانی

**توضیحات:**
- ادمین قالب‌های پیام رو مدیریت میکنه
- قالب‌ها برای ارسال پیامک و اعلان استفاده میشن

---

## 8️⃣ تنظیمات سیستم

### Setting (تنظیمات)
- `id` - شناسه یکتا
- `key` - کلید تنظیم (یکتا)
- `value` - مقدار (JSON برای مقادیر پیچیده)
- `type` - نوع (string, number, boolean, json)
- `category` - دسته‌بندی (general, payment, exam, ...)
- `description` - توضیحات
- `updated_by` - بروزرسانی شده توسط (admin)
- `updated_at` - تاریخ بروزرسانی

**توضیحات:**
- تنظیمات عمومی سیستم در این جدول نگهداری میشن

### Role (نقش)
- `id` - شناسه یکتا
- `name` - نام نقش
- `permissions` - دسترسی‌ها - JSON
- `description` - توضیحات
- `created_at` - تاریخ ایجاد
- `updated_at` - تاریخ بروزرسانی

**توضیحات:**
- نقش‌های مختلف و دسترسی‌هاشون
- فعلاً سه نقش داریم: admin, owner, manager

---

## 9️⃣ سایر

### AuditLog (لاگ عملیات)
- `id` - شناسه یکتا
- `user_id` - شناسه کاربر (ارتباط با User)
- `action` - عملیات انجام شده
- `entity_type` - نوع موجودیت (User, Company, ...)
- `entity_id` - شناسه موجودیت
- `old_values` - مقادیر قبلی (اختیاری) - JSON
- `new_values` - مقادیر جدید (اختیاری) - JSON
- `ip_address` - آی‌پی کاربر
- `user_agent` - مرورگر کاربر
- `created_at` - تاریخ عملیات

**توضیحات:**
- تمام عملیات مهم در سیستم لاگ میشن
- برای امنیت و ممیزی استفاده میشه

---

## 📊 خلاصه جداول

**تعداد کل جداول:** 28

### تقسیم‌بندی بر اساس دسته:
- **کاربران و احراز هویت:** 1 جدول (User)
- **سازمان و متقاضیان:** 2 جدول (Company, Manager)
- **نیازسنجی:** 4 جدول (AssessmentTemplate, AssessmentStep, AssessmentQuestion, Assessment)
- **آزمون‌ها:** 7 جدول (Exam, Question, ExamQuestion, ExamSet, ExamSetItem, ExamResult)
- **امور مالی:** 5 جدول (Payment, Invoice, FinancialDocument, Bill, BillItem)
- **ارتباطات:** 5 جدول (Notification, SupportTicket, SupportMessage, SMSLog, MessageTemplate)
- **تنظیمات:** 2 جدول (Setting, Role)
- **سایر:** 1 جدول (AuditLog)

---

## 🔗 روابط مهم بین جداول

1. **User** ↔ **Company** (یک کاربر owner میتونه یک سازمان داشته باشه)
2. **Company** ↔ **Manager** (یک سازمان چند متقاضی داره)
3. **Manager** ↔ **Assessment** (یک متقاضی چند نیازسنجی میتونه داشته باشه)
4. **AssessmentTemplate** ↔ **AssessmentStep** ↔ **AssessmentQuestion** (ساختار سلسله مراتبی)
5. **Assessment** ↔ **ExamSet** (بعد از تکمیل نیازسنجی، آزمون تخصیص داده میشه)
6. **ExamSet** ↔ **Exam** (یک مجموعه شامل چند آزمون)
7. **Exam** ↔ **Question** (یک آزمون شامل چند سوال)
8. **Manager** ↔ **Payment** (هر متقاضی چند پرداخت میتونه داشته باشه)
9. **Payment** ↔ **Invoice** (هر پرداخت میتونه یک فاکتور داشته باشه)
10. **Bill** ↔ **FinancialDocument** (هر صورتحساب شامل چند سند مالی)

---

**نکته مهم:** این موجودیت‌ها براساس کد فرانت‌اند و mock data های موجود طراحی شدن. در پیاده‌سازی بک‌اند ممکنه نیاز باشه بعضی از فیلدها یا روابط تغییر کنن یا بهینه‌سازی بشن.
